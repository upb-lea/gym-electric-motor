Constraint Monitor
##################

.. toctree::
   :maxdepth: 1
   :caption: Available Constraints:

   constraints/constraint
   constraints/limit_constraint
   constraints/squared_constraint


Usage Guide
___________

ToDo


Constraint Monitor API Documentation
____________________________________

.. note::
   This page is present as requested, but the auto-generated API section is
   **temporarily disabled** because the upstream docstring in
   ``gym_electric_motor.core.ConstraintMonitor`` causes a Sphinx parsing error
   (``Unexpected indentation``). Once that docstring is cleaned up, we will
   re-enable the block below.

.. code-block:: rst

   .. autoclass:: gym_electric_motor.core.ConstraintMonitor
      :members:
      :inherited-members:
      :show-inheritance:
      :member-order: groupwise


